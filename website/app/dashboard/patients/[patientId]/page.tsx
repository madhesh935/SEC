import {redirect} from "next/navigation";export default async function Page({params}:{params:Promise<{patientId:string}>}){const {patientId}=await params;redirect("/caregiver/patients/"+encodeURIComponent(patientId));}
